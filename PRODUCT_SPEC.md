# DocViewKit Omni 浏览器扩展产品规范

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | DocViewKit Omni |
| 交付形态 | 浏览器扩展 |
| 文档版本 | 0.1 |
| 状态 | Draft |
| 更新日期 | 2026-08-25 |
| 目标仓库 | `docviewkitOmniForBrowser` |
| 首发平台 | Chrome Desktop、Firefox Desktop、Safari for macOS |

本文档是产品范围、跨浏览器架构、隐私安全和发布验收的共同依据。实现可以演进，但不得绕过本文规定的只读、本地处理与最小权限边界。

## 2. 产品定义

DocViewKit Omni 是浏览器内的只读文档预览产品。用户可以从扩展入口选择或拖入本地文档，也可以对网页中的文档链接主动发起预览，无需安装桌面 Office 软件，也无需把文件上传到第三方转换服务。

产品覆盖三类浏览器引擎：

- Chrome：Chromium / Blink。
- Firefox：Gecko。
- Safari：WebKit；通过 Safari Web Extension 包装共享扩展资源。

文档解析与渲染只由固定版本的 DocViewKit Viewer 构建产物提供。本仓库只实现浏览器宿主接入、权限、生命周期与打包，不实现第二套解析器或渲染器。

### 核心价值

- 在浏览器标签页中直接查看真实文档内容与结构。
- 本地文件不离开设备；远程文档只从用户选定的原始地址读取。
- 三个浏览器保持一致的格式能力、预览行为和诊断语义。
- 以按需权限代替安装时全站访问。

## 3. 目标与非目标

### 3.1 产品目标

1. 在 Chrome、Firefox 与 Safari 桌面版中提供一致、稳定的只读文档预览。
2. 使用一套共享源码和一份 Viewer 产物，避免三端行为与格式能力漂移。
3. 默认离线处理本地文件，远程链接只在用户明确操作后读取。
4. 在文档局部损坏时尽量保留其余可用内容和可复制诊断。

### 3.2 用户目标

- 从扩展按钮快速打开一个独立预览标签页。
- 通过文件选择或拖放查看本机文档。
- 从网页文档链接的上下文菜单进入预览。
- 明确知道扩展请求了什么权限、文档是否离开原始来源。

### 3.3 首发不包含

- 自动接管所有文档导航、下载或浏览器内置 PDF 查看器。
- 安装时申请 `<all_urls>`、读取浏览历史或持续观察网页内容。
- 文档编辑、批注、协作、格式转换、打印增强和下载管理。
- 云端存储、同步、账户、后台服务、遥测和广告。
- AI 摘要、问答、翻译或内容生成。
- Android、iOS、iPadOS 和非目标浏览器的正式支持承诺。
- 用缩略图、封面图或系统预览图冒充最终渲染结果。

自动接管链接只有在用户研究证明手动入口不足，且三端能以可理解的按需权限实现后再评估。

## 4. 目标用户与核心场景

### 4.1 目标用户

- 在浏览器中接收、审阅办公文档和 PDF 的个人用户。
- 需要避免把敏感文档上传到在线转换网站的技术、法务、财务和运营人员。
- 在不同桌面浏览器间切换，希望获得一致阅读体验的团队成员。

### 4.2 核心场景

1. 用户点击工具栏扩展按钮，在新标签页进入预览页并选择本地文档。
2. 用户将本地文档拖入预览页，扩展直接读取浏览器交付的 `File` 对象。
3. 用户右键网页中的文档链接并选择“使用 DocViewKit Omni 预览”。
4. 文档包含局部损坏或未知资源时，其余可用页面、工作表、幻灯片或内容继续显示。
5. 用户复制脱敏诊断以便排查问题，不暴露正文、完整文件名或本机路径。

## 5. 首发范围

| 编号 | 能力 | 优先级 | 首发要求 |
| --- | --- | --- | --- |
| F-01 | 扩展入口 | P0 | 点击工具栏按钮打开或聚焦独立预览标签页 |
| F-02 | 本地文件 | P0 | 文件选择与拖放；字节仅在扩展页内处理 |
| F-03 | 链接预览 | P0 | 仅由用户点击文档链接上下文菜单触发 |
| F-04 | 按需来源权限 | P0 | 只请求目标链接精确来源，不在安装时申请全站权限 |
| F-05 | 真实内容预览 | P0 | 使用固定且可校验的 DocViewKit Viewer 构建产物 |
| F-06 | Viewer 操作 | P0 | 复用 Viewer 已有的缩放、导航、搜索和视图能力 |
| F-07 | 错误与诊断 | P0 | 区分不支持、损坏、加密、网络、权限和资源限制 |
| F-08 | 本地与离线 | P0 | 运行时静态资源随扩展打包，不加载远程代码 |
| F-09 | 三端一致性 | P0 | 格式清单、消息契约、Viewer 版本和文件哈希一致 |
| F-10 | 隐私说明 | P0 | 三个商店页面与扩展内说明保持一致 |
| F-11 | 主题与语言 | P1 | 跟随浏览器/系统深浅主题；界面支持中文和英文 |
| F-12 | 诊断导出 | P1 | 用户主动导出不含正文、路径与持久标识的摘要 |
| F-13 | Safari 移动端 | P2 | 完成触控、文件选择、内存和 App Store 验收后再支持 |

## 6. 用户流程与验收

### 6.1 本地文件预览

1. 用户点击扩展按钮。
2. 浏览器打开或聚焦扩展预览页。
3. 用户选择或拖入一个文件。
4. 扩展把 `File` 交给 Viewer，并在首个有意义内容可见后进入就绪状态。

**验收：**

- **Given** 用户选择支持格式的健康本地文档
- **When** Viewer 完成解析
- **Then** 独立标签页显示真实解析结果
- **And** 没有文档上传、远程运行时代码请求或本机路径读取。

### 6.2 网页链接预览

1. 用户对链接打开上下文菜单。
2. 扩展验证协议，下载后由 Viewer 识别格式。
3. 若当前来源尚未授权，浏览器显示精确来源权限请求。
4. 授权后扩展从原始 `http` 或 `https` 地址读取字节并在预览页打开。

**验收：**

- **Given** 用户主动选择一个可访问的文档链接
- **When** 用户批准该来源权限
- **Then** 扩展只联系该文档的原始请求链
- **And** 重定向到未授权来源时停止并重新请求权限，不静默扩大访问范围。

### 6.3 局部损坏

- **Given** 文档包含可隔离的损坏对象或资源
- **When** Viewer 能继续解析其他内容
- **Then** 其余内容继续显示
- **And** 用户可复制对应结构化诊断。

### 6.4 权限拒绝

- **Given** 用户拒绝远程来源访问
- **When** 权限请求结束
- **Then** 预览页说明未读取该文档并允许重试
- **And** 不引导用户授予更宽权限。

## 7. 支持格式规则

1. 支持格式的唯一事实来源是 Viewer 发布产物中的机器可读清单。
2. 三个目标清单、上下文菜单判断、商店文案和测试矩阵必须从该清单生成或在构建时与其校验。
3. 扩展名只是预筛选；最终格式识别由 Viewer 基于真实字节完成。
4. 格式包缺失或校验失败必须作为打包错误处理，不得退回浏览器缩略图。
5. 正式实现使用三端验证后固定版本与校验和的 `@docviewkit/viewer@0.2.65`。

## 8. 仓库与模块结构

```text
docviewkitOmniForBrowser/
├── AGENTS.md
├── PRODUCT_SPEC.md
├── README.md
├── src/                 # 共享页面、后台事件、消息与权限行为
├── targets/
│   ├── chrome/          # Manifest V3 与 Chrome Web Store 元数据
│   ├── firefox/         # Gecko 清单键、签名与 AMO 元数据
│   └── safari/          # Safari 清单差异与包装配置
├── viewer/              # Viewer 锁定信息、宿主接口和完整性清单
└── test/                # 契约、浏览器、权限、安全和包一致性测试
```

### 8.1 共享源码

`src/` 是功能行为的唯一实现位置，包括：

- 扩展预览页与 Viewer 宿主桥接。
- 本地文件选择和拖放。
- 工具栏入口、上下文菜单与预览标签页复用。
- 精确来源权限请求、远程读取、取消和生命周期释放。
- 主题、语言、错误和诊断展示。

共享源码只使用目标浏览器共同支持的 WebExtension/Web API。确有差异时，在构建边界注入最小清单或 API 适配，不复制业务流程。

### 8.2 浏览器目标

- `targets/chrome/`：Chrome Manifest V3 清单差异和商店元数据。
- `targets/firefox/`：`browser_specific_settings.gecko`、签名和 AMO 所需隐私声明；不得复制共享脚本。
- `targets/safari/`：Safari 特有清单差异和包装参数。发布时从同一构建目录生成 Safari Web Extension/Xcode 包装，生成目录不作为手写源码维护。

### 8.3 Viewer 产物

每次发布必须固定：

- npm 包名、精确版本与 lockfile integrity。
- 宿主接口主版本。
- Viewer HTML、CSS、JavaScript、Wasm、字体、格式包和许可证文件。
- 每个文件的 SHA-256 与机器可读格式清单。

三个目标包必须包含相同的 Viewer 文件集合和哈希。禁止引用 `latest`、远程 CDN、Git submodule 或分别修改的浏览器副本。

## 9. 运行时设计

### 9.1 最小扩展表面

- 一个工具栏 action。
- 一个独立扩展预览页。
- 一个只对链接显示的上下文菜单项。
- 一个按事件唤醒的后台上下文，用于入口、标签页和权限协调。

首发不注入 content script，不创建侧边栏，不常驻轮询，不监控浏览历史。

### 9.2 消息语义

后台上下文到预览页：

- `open-local`：由预览页持有的本地 `File` 直接打开，不序列化路径。
- `open-remote`：传递经过验证的 URL 和一次预览请求标识。
- `cancel`：取消过时读取或解析。
- `theme`、`locale`、`dispose`：同步显示与释放资源。

预览页到后台上下文：

- `ready`、`document-ready`、`diagnostic`、`permission-required`、`fatal-error`。

消息必须带接口版本。未知消息安全忽略并记录诊断，主版本不兼容时构建失败。

### 9.3 文件与网络处理

- 本地文件仅通过用户选择/拖放获得的 `File` 读取，不尝试解析裸路径。
- 远程链接允许 `http` 与 `https`，并只在用户主动预览后请求精确来源权限。
- 权限按 URL 来源请求，重定向每跨越一个新来源都重新验证。
- 不把大文件 Base64 化；优先传递 `Blob`、`File`、`ArrayBuffer` 或受控扩展 URL。
- 远程响应字节默认仅在内存中保留，标签页关闭或新任务开始时释放。
- 取消旧任务，防止较早响应覆盖最新预览。

## 10. 浏览器平台边界

### 10.1 Chrome

- 使用 Manifest V3；后台逻辑按 service worker 可随时终止设计。
- 不依赖远程托管代码，安装权限保持最小。
- 首发验证当前稳定版及前一个稳定版桌面 Chrome。

### 10.2 Firefox

- 使用 Firefox 当前支持的 WebExtensions Manifest V3 能力。
- 清单包含 AMO 签名所需扩展 ID 和数据收集声明；首发声明不收集数据。
- 后台入口差异通过目标清单解决，不分叉共享业务行为。
- 首发验证当前 ESR 与当前稳定版桌面 Firefox。

### 10.3 Safari

- 使用 Safari Web Extension；从共享构建目录通过 Apple 官方打包工具生成包装。
- 首发仅承诺 macOS Safari；iOS/iPadOS 必须另行完成移动端资源和交互验收。
- 记录打包工具对清单键和 API 的所有警告；P0 能力存在不兼容时不得发布。
- 首发验证当前 macOS 正式版及其上一主要版本可用的 Safari。

## 11. 用户体验要求

- 工具栏按钮只负责进入预览，不增加弹窗式功能中心。
- 文件选择区同时支持键盘操作和拖放；焦点、标签页标题与关闭行为遵循浏览器习惯。
- Viewer 已有操作不在扩展外壳重复实现。
- 加载中显示可取消状态，不以文档内嵌预览图伪装完成。
- 权限提示先说明“读取哪个来源、为了什么”，拒绝后保持可用的本地文件入口。
- 深浅主题只改变应用外壳，不改写文档定义的颜色。
- 界面、上下文菜单、错误和商店说明至少提供中文与英文。

## 12. 安全、隐私与可靠性

- 扩展页面采用严格 CSP，脚本、样式、Wasm 和字体仅来自扩展包。
- 禁止 `eval`、动态远程模块和未经校验的运行时资源。
- 不采集或发送文档正文、文件名、路径、URL、哈希、截图、浏览记录和设备标识。
- 远程文档请求是用户选择的源站读取，不得转发到 DocViewKit 或第三方服务器。
- 文档超链接只允许明确批准的协议，并交由浏览器新标签页处理；阻止脚本和特权协议。
- 沿用 Viewer 对压缩炸弹、异常尺寸、超大资源和解析耗时的限制，不得由 adapter 放宽。
- 单个标签页或单个文档失败不得破坏其他预览会话。
- 每个目标包包含许可证、第三方通知、权限用途和隐私说明。

## 13. 性能与资源门槛

- adapter 引入的首次可见预览额外耗时不超过对应 Viewer 浏览器基准的 15%。
- 同一扩展进程内可复用不可变运行时资源，但不得跨会话保留文档字节。
- 新预览开始时取消旧下载和解析；标签页关闭后释放文档、对象 URL、监听器和缓存。
- 后台上下文不得依赖常驻内存状态，恢复后能从预览页或明确持久状态重建最小会话。
- 构建与测试必须能在干净检出、没有 Viewer 源码的环境中完成。

## 14. 测试与发布门槛

### 14.1 共享门槛

- Viewer 版本、integrity、接口版本、逐文件 SHA-256 和格式清单校验。
- 三个目标包的 Viewer 文件集合与哈希完全一致。
- 工具栏入口、本地文件、链接预览、权限拒绝、重定向、取消和销毁路径测试。
- 健康、局部损坏、整体失败、加密和资源限制真实文档夹具。
- 网络断言：本地预览零外部请求；远程预览只访问已授权请求链。
- 权限断言：发布清单没有 `<all_urls>`、浏览历史和非必要权限。
- CSP、外链协议、文件名注入和未知消息安全测试。

### 14.2 浏览器门槛

- Chrome：未打包加载、真实文档冒烟、service worker 重启和商店 ZIP 检查。
- Firefox：临时安装、真实文档冒烟、ESR/稳定版兼容和 AMO lint/signing 检查。
- Safari：官方工具转换/打包、清单警告审查、macOS Safari 真实文档冒烟和签名包检查。

### 14.3 发布完成定义

某一浏览器发布只有在以下条件全部满足后完成：

1. 对应提交和版本标签已推送。
2. 共享门槛与该浏览器门槛通过。
3. 商店中的目标版本可公开安装。
4. 商店下载包的 Viewer 版本和哈希与发布清单一致。
5. 从商店安装的真实包完成最小冒烟测试。

三个商店独立发布，不因审核时间强制同步；兼容矩阵必须记录各商店版本与 Viewer 版本。

## 15. 成功指标

### 15.1 质量指标

| 指标 | 首发门槛 |
| --- | --- |
| 受控健康文档打开成功率 | 100% |
| 指定局部损坏夹具保留可用内容 | 100% |
| 三端 Viewer 产物一致性 | 版本与 SHA-256 完全一致 |
| 本地预览外部网络请求 | 0 |
| 安装时全站权限 | 0 |
| 阻断级安全问题 | 0 |

### 15.2 产品指标

- 三个商店的有效安装与主动卸载反馈。
- “无法打开文档”反馈占比及其中可复制诊断覆盖率。
- 权限请求拒绝率和远程链接预览完成率；统计仅来自商店聚合数据或用户主动反馈，首发不内置遥测。
- 商店评分、崩溃报告和经验证的格式兼容问题。

## 16. 风险与措施

| 风险 | 影响 | 措施 |
| --- | --- | --- |
| 三端 Manifest V3/API 差异 | 相同行为在单端失效 | 共享业务源码，差异限制在目标清单/API 适配并做真实浏览器契约测试 |
| Safari 包装与审核链更长 | 发布不同步 | 从同一构建产物生成包装，独立版本与发布状态 |
| 远程站点鉴权、CORS 或重定向 | 链接无法读取 | 精确来源权限、明确诊断；不以代理服务绕过 |
| 大文件导致内存峰值 | 标签页崩溃 | 二进制传递、任务取消、资源预算和关闭释放 |
| Viewer 产物漂移 | 格式或行为不一致 | 固定版本、integrity、逐文件哈希和包后校验 |
| 权限范围扩张 | 用户不信任或商店拒审 | 默认本地、按需来源权限、发布清单自动断言 |

## 17. 里程碑

### M1：共享接口与构建

- [ ] 固定首个 Viewer 版本、接口与完整性清单。
- [ ] 生成三个最小目标清单与同源构建目录。
- [ ] 建立包一致性、权限和 CSP 契约测试。

### M2：本地文件 MVP

- [ ] 完成工具栏入口、文件选择、拖放和预览页。
- [ ] 在 Chrome、Firefox、Safari 打开同一组真实文档。
- [ ] 验证离线运行和零外部请求。

### M3：远程链接预览

- [ ] 完成上下文菜单、精确来源权限和重定向保护。
- [ ] 完成取消、超时、鉴权失败和资源限制诊断。
- [ ] 三端权限文案和行为验收通过。

### M4：三商店发布

- [ ] Chrome Web Store、Firefox AMO、Apple App Store 包通过审核。
- [ ] 商店安装包哈希和真实文档冒烟通过。
- [ ] README、隐私说明、支持格式与商店文案一致。

## 18. 待确认事项

以下事项不阻塞仓库骨架，但必须在对应里程碑前确定：

1. 商店开发者账户、扩展 ID、Apple bundle identifier 与签名主体。
2. 商业许可模式以及 Viewer 授权状态在扩展内的展示方式。
3. 远程文档是否需要支持带登录态的源站；首发默认不读取或转发浏览器 Cookie。
4. Safari iOS/iPadOS 是否进入首发后的下一阶段。

## 19. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-25 | 0.1 | 建立三浏览器共享架构、首发范围、安全边界与验收门槛 |
