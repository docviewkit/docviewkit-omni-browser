# 项目规则

- 产品名称统一为 `DocViewKit Omni`；本仓库负责 Chrome、Firefox 与 Safari 桌面浏览器中的只读文档预览扩展。
- 三个浏览器必须复用 `src/` 中的同一套 WebExtension 行为。浏览器差异只允许存在于 `targets/` 的清单、打包和商店元数据中。
- DocViewKit Viewer 与渲染内核保持独立，通过固定版本、完整性信息和逐文件 SHA-256 的构建产物集成。禁止复制 Viewer 源码、使用 Git submodule 或为不同浏览器修改三份产物。
- 最终预览必须来自 DocViewKit 对文档真实内容与结构的解析和渲染，禁止使用缩略图、封面图或浏览器生成的预览图作为最终结果或解析失败兜底。
- 遵循“最佳努力渲染、局部失败隔离、整体可用优先”；可隔离的单页、单对象或单资源错误不得升级为整个文件失败，所有降级必须保留可复制诊断。
- 文档默认只在扩展页面本地处理。不得上传文件、发送文件名或路径、加载远程脚本，首发不得采集遥测。
- 权限最小化：不得在安装时申请全站访问。远程文档只在用户主动发起预览后请求精确来源权限。
- 使用浏览器官方扩展、文件选择和下载接口，不绕过浏览器权限、CSP、焦点、快捷键和生命周期约束；不得直接信任任意 `file://` 路径。
- 每个非平凡共享行为至少保留一个可运行回归，并在 Chrome、Firefox、Safari 中通过契约或真实浏览器测试。
- 保持最少依赖、最少目录和最薄 adapter。未经产品规范要求，不新增后台服务、账户、编辑、批注、转换或 AI 功能。
